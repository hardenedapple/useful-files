Add automatic version checking:

inputrc -- vi-forward-bigword, vi-backward-bigword
           is newer version of
           vi-fWord,           vi-bWord


tmux.conf -- set-option -g pane-active-border-style fg=white
             set-option -g pane-border-style fg=green
             is newer version of
             set-option -g pane-active-border-fg white  
             set-option -g pane-border-fg green        

             M-{1,2,3,4,5} mappings for select-layout have to be added in
             earlier versions
